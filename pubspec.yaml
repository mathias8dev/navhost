name: navhost
description: A Compose-inspired declarative navigation wrapper for Flutter's Navigator 2.0 with NavController, NavHost, transitions, interceptors, and bottom sheet/dialog support.
version: 0.1.3
homepage: https://github.com/mathias8dev/navhost
repository: https://github.com/mathias8dev/navhost

environment:
  sdk: ^3.11.3
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - navigation
  - navigator
  - router
  - compose

flutter:
